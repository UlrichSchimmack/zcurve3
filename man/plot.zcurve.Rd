% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.zcurve}
\alias{plot.zcurve}
\title{Plot a fitted z-curve}
\usage{
\method{plot}{zcurve}(
  x,
  histogram_col = "blue3",
  curve_col = "red",
  histogram_width = 0.2,
  local_interval = 0.5,
  xlim = c(0, 6),
  ylim = c(0, 0.6),
  ylab = "Density",
  show_text = TRUE,
  show_local_power = TRUE,
  line_width = 4,
  text_size = 1,
  show_ci_band = TRUE,
  show_edr_band = FALSE,
  ci_level = 0.95,
  ci_band_col = "grey70",
  edr_band_col = "darkorange",
  ...
)
}
\arguments{
\item{x}{A \code{zcurve} object from \code{\link[=zcurve]{zcurve()}}.}

\item{histogram_col}{Colour of the histogram bars.}

\item{curve_col}{Colour of the fitted density curve.}

\item{histogram_width}{Histogram bar width.}

\item{local_interval}{z-interval width for the local-power annotations.}

\item{xlim, ylim}{Axis ranges (\code{ylim = c(0, ymax)} sets the top of the y-axis).}

\item{ylab}{y-axis label.}

\item{show_text}{Logical; show the ODR / EDR / ERR / FDR estimate block.}

\item{show_local_power}{Logical; annotate local power under the curve.}

\item{line_width}{Fitted-curve thickness.}

\item{text_size}{Multiplier for text and point sizes.}

\item{show_ci_band}{Logical; draw the pointwise bootstrap density band (needs
\code{boot_iter > 0}).}

\item{show_edr_band}{Logical; overlay the fitted curves at the extreme EDR
bootstrap replicates.}

\item{ci_level}{Band / interval level.}

\item{ci_band_col, edr_band_col}{Band colours.}

\item{...}{Further graphical arguments passed to the underlying plot.}
}
\value{
\code{x}, invisibly; called for the plot it draws. Options stored on the
fit via \code{zcurve(plot_control = ...)} are used as defaults, and explicit
arguments here override them.
}
\description{
Draws the histogram of significant z-values, the fitted density, and the
estimate block (ODR / EDR / ERR / FDR).
}
\examples{
fit <- zcurve(zval = abs(rnorm(2000, 2.5)), show_plot = FALSE)
plot(fit, ylim = c(0, 0.5))
}
\seealso{
\code{\link[=zcurve]{zcurve()}}, \code{\link[=zcurve_forest]{zcurve_forest()}}
}
