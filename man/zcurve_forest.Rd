% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{zcurve_forest}
\alias{zcurve_forest}
\title{Minimum-effect forest plot of individual studies}
\usage{
zcurve_forest(
  object,
  min_effect = NULL,
  z_min = 4,
  es_lb_min = 0.2,
  es_lb_max = Inf,
  k_max = 30,
  sort_by = c("lower_bound", "prob_meaningful", "adjusted_effect", "z", "study_id"),
  decreasing = TRUE,
  show_raw = TRUE,
  xlim = NULL,
  digits = 2,
  point_col = "blue3",
  raw_col = "grey60",
  min_effect_col = "red3",
  labels = NULL,
  label_id = NULL,
  label_chars = 50,
  show_id = TRUE,
  stat = c("power", "z"),
  label_max_frac = 0.5,
  forest_frac = 0.26,
  text_size = 1,
  panel_widths = c(1.9, 5.7, 1.9),
  file = NULL,
  width = NULL,
  height = NULL,
  res = 120,
  row_height = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{zcurve} object fit with \code{yi}/\code{sei} and \code{boot_iter > 0}.}

\item{min_effect}{Minimum effect of interest (the dashed line and
\verb{P(|d| > min_effect)}). Can be changed here without refitting; defaults to
the fit's \code{min_effect}.}

\item{z_min}{Show only studies with \verb{|z| > z_min}.}

\item{es_lb_min, es_lb_max}{Keep only studies whose adjusted-effect lower bound
lies in \verb{[es_lb_min, es_lb_max]}.}

\item{k_max}{Show at most this many studies (the top \code{k_max} by \code{sort_by}).}

\item{sort_by}{Sort key: one of \code{"lower_bound"}, \code{"prob_meaningful"},
\code{"adjusted_effect"}, \code{"z"}, \code{"study_id"}.}

\item{decreasing}{Logical; sort direction.}

\item{show_raw}{Logical; draw observed (unadjusted) effects as open circles.}

\item{xlim}{Effect-axis range (auto when \code{NULL}).}

\item{digits}{Rounding in the interval column.}

\item{point_col, raw_col, min_effect_col}{Colours for the adjusted
point/interval, the observed circles, and the minimum-effect line.}

\item{labels}{Descriptive row labels replacing the study id. Supply either a
vector \strong{named by \code{study_id}}, a plain vector \strong{paired with \code{label_id}},
or \strong{one label per study in fit order}. Long labels are cut at the first
\code{":"} and packed to whole words. If a named vector is given but none of the
names match a \code{study_id}, the ids are shown and a warning is issued.}

\item{label_id}{Optional vector of \code{study_id}s, one per \code{labels} entry, so you
can write \verb{labels = titles, label_id = ids} instead of
\code{labels = setNames(titles, ids)}.}

\item{label_chars}{Maximum label length in characters (\code{NULL} leaves labels
untouched).}

\item{show_id}{Logical; draw a separate id column of \code{study_id}s.}

\item{stat}{Statistic shown beside each effect: \code{"power"} (adjusted
replication probability, the default) or \code{"z"} (observed z).}

\item{label_max_frac}{Cap on the label column as a fraction of figure width.}

\item{forest_frac}{Minimum share of the figure width reserved for the forest
panel; labels are truncated to keep it.}

\item{text_size}{Multiplier for all row/header text and point sizes.}

\item{panel_widths}{Fallback relative widths (labels, forest, estimates), used
only when the automatic column sizing is unavailable.}

\item{file}{Optional output path. When given, the function opens a device
auto-sized to the study count (so rows stay readable) and writes the plot;
the format comes from the extension (\code{png}, \code{jpeg}, \code{tiff}, \code{pdf}, \code{svg}).}

\item{width, height}{Device size in inches for \code{file} output (height auto-sizes
to the number of rows when \code{NULL}).}

\item{res}{Resolution (pixels per inch) for raster \code{file} output.}

\item{row_height}{Inches per row for the auto-sized height (\code{NULL} derives it
from \code{text_size}).}

\item{...}{Further graphical arguments.}
}
\value{
The plotted data frame (including \code{study_id} and \code{cluster_id}),
invisibly.
}
\description{
Draws, for each individual study, its shrinkage-adjusted effect size (the
observed effect pulled toward the fitted mixture) with a credible interval,
alongside its adjusted power (or observed z) and the minimum-effect line.
Requires a bootstrapped fit (\code{boot_iter > 0}) with \code{yi}/\code{sei} supplied, so the
adjusted-effect intervals reflect the uncertainty in the fitted population,
not just each study's own standard error.
}
\details{
Columns drawn: an optional id column, the study labels, the forest (adjusted
effect with its interval, observed effect as an open circle, and the
minimum-effect line), and an estimates column (adjusted power or z, then the
adjusted effect with its interval). The forest panel is auto-sized; long
labels are truncated to keep it at least \code{forest_frac} of the width.
}
\examples{
set.seed(1)
fit <- zcurve(yi = abs(rnorm(300, 0.5, 0.3)), sei = runif(300, 0.1, 0.2),
              boot_iter = 100, show_plot = FALSE,
              control = zcurve_control(parallel = FALSE))
zcurve_forest(fit, z_min = 3, es_lb_min = 0.1)
}
\seealso{
\code{\link[=zcurve]{zcurve()}}, \code{\link[=plot.zcurve]{plot.zcurve()}}
}
