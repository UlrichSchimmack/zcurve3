% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zcurve.R
\name{zcurve_control}
\alias{zcurve_control}
\title{Tuning parameters for \code{\link[=zcurve]{zcurve()}}}
\usage{
zcurve_control(
  boot_iter = 0,
  parallel = TRUE,
  cores = NULL,
  seed = NULL,
  tol_criterion = 1e-06,
  max_iter = 2000,
  max_iter_boot = 500,
  ci_alpha = 0.05,
  augment = TRUE,
  n_bars = 512,
  density_step = 0.01,
  bootstrap_density_step = 0.05,
  bw_est = 0.05,
  spline_width = 0.5,
  floor_correct = TRUE,
  floor_reps = 5,
  es_null_share = 0.5
)
}
\arguments{
\item{boot_iter}{Bootstrap replicates (500+ for final models). Set here \strong{or}
on \code{\link[=zcurve]{zcurve()}}, not both.}

\item{parallel}{Logical; run the bootstrap in parallel. Default \code{TRUE}.}

\item{cores}{Number of worker processes, or \code{NULL} to auto-pick a conservative
count.}

\item{seed}{Optional integer seed for a reproducible bootstrap.}

\item{tol_criterion}{EM convergence tolerance.}

\item{max_iter, max_iter_boot}{EM iteration caps for the point fit and for each
bootstrap replicate.}

\item{ci_alpha}{Alpha for reported confidence intervals (\code{0.05} = 95\%).}

\item{augment, n_bars}{Density-method (OF) tuning: lower-bound bias correction
and the density-estimation resolution.}

\item{density_step, bootstrap_density_step}{Numerical density-grid step for the
point fit and (coarser) for the bootstrap fits.}

\item{bw_est, spline_width}{Kernel bandwidth and the blend of truncated and
normal kernel densities for the OF fit.}

\item{floor_correct}{Logical; remove the homogeneous-null floor from the
effect-size heterogeneity \code{es_tau_sig}. Default \code{TRUE}.}

\item{floor_reps}{Homogeneous-null refits averaged to estimate that floor.}

\item{es_null_share}{Individual-effects prior only: give the null component
(ncp 0) at least this share of the low-power (ncp <= 1) mass, so
genuinely-null studies can shrink to 0. Applied to the population weights for
the individual posterior only; the fit's EDR/ERR/FDR are untouched. \code{0}
disables.}
}
\value{
A named list of control parameters for \code{zcurve(control = ...)}.
}
\description{
Collects fitting, bootstrap, and effect-size tuning options into a list to be
passed to \code{zcurve(control = ...)}.
}
\seealso{
\code{\link[=zcurve]{zcurve()}}
}
