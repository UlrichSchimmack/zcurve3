% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zcurve.R
\name{zcurve}
\alias{zcurve}
\title{Fit a z-curve to significant test statistics}
\usage{
zcurve(
  zval = NULL,
  tval = NULL,
  df = NULL,
  pval = NULL,
  n = NULL,
  yi = NULL,
  sei = NULL,
  study_id = NULL,
  cluster_id = NULL,
  est_method = "OF",
  directional = FALSE,
  folded = TRUE,
  alpha = 0.05,
  crit = qnorm(1 - alpha/2),
  int_beg = crit,
  int_end = 6,
  edr_ci_adj = 0.05,
  err_ci_adj = 0.03,
  min_effect = 0.2,
  min_int = 20,
  ncp = 0:6,
  k_ncp = length(ncp),
  z_sd = rep(1, k_ncp),
  z_sd_fixed = TRUE,
  ncp_fixed = TRUE,
  w_fixed = FALSE,
  x_lim_min = 0,
  x_lim_max = 6,
  int_loc = 1,
  version = "3S",
  date = "2026.07.22",
  show_plot = TRUE,
  show_summary = FALSE,
  boot_iter = 0,
  control = zcurve_control(),
  plot_control = list()
)
}
\arguments{
\item{zval}{Numeric vector of z-values (the native input).}

\item{tval}{Numeric vector of t-values; requires \code{df}.}

\item{df}{Degrees of freedom for \code{tval} (scalar or vector).}

\item{pval}{Numeric vector of two-sided p-values (converted to z internally).}

\item{n}{Sample sizes, used with some inputs to derive standard errors.}

\item{yi}{Observed effect sizes. Enables the effect-size estimates and the
forest plot; z is taken as \code{yi / sei} when test statistics are not otherwise
supplied.}

\item{sei}{Standard errors of \code{yi}.}

\item{study_id}{Optional identifier labelling each individual test (one forest
row per test).}

\item{cluster_id}{Optional identifier grouping dependent tests; the bootstrap
then resamples whole clusters so dependence does not deflate the intervals.}

\item{est_method}{Estimation method: \code{"OF"} (density/KDE, the default),
\code{"EM"} (likelihood mixture), or \code{"SQP"}.}

\item{directional}{Logical; if \code{TRUE} the sign of the test statistic is
retained. Default \code{FALSE}.}

\item{folded}{Logical; if \code{TRUE} (default) input is folded to \verb{|z|} so both
significant tails inform the fit.}

\item{alpha}{Significance threshold. Default \code{0.05}.}

\item{crit}{Two-sided critical value; derived from \code{alpha}.}

\item{int_beg}{Lower bound of the selection window; derived from \code{crit}.
Negative \code{ncp} components are only identified when \code{int_beg <= 0} (no
selection).}

\item{int_end}{Right-truncation boundary of the modelling grid; statistics
beyond it are treated as power 1.}

\item{edr_ci_adj, err_ci_adj}{Small adjustments to the EDR / ERR confidence
bounds.}

\item{min_effect}{Smallest effect size of practical interest (the forest
plot's minimum-effect line and \verb{P(|d| > min_effect)}).}

\item{min_int}{Minimum number of significant test statistics required to fit.
Default \code{20}; lower it (with caution — estimates get noisy) to fit a small
set.}

\item{ncp}{Component locations (non-centralities). Default \code{0:6}.}

\item{k_ncp}{Number of components; defaults to \code{length(ncp)}.}

\item{z_sd}{Component standard deviations, one per \code{ncp}, each \verb{>= 1}.}

\item{z_sd_fixed, ncp_fixed, w_fixed}{Logicals holding the component SDs,
locations, or weights fixed during EM estimation.}

\item{x_lim_min, x_lim_max}{Lower/upper bound of the modelling grid (and the
plot x-axis).}

\item{int_loc}{Bin width for local-power / local-effect reporting; \code{0} disables
it.}

\item{version, date}{Strings shown on the plot; set to \code{NULL} or \code{""} to
suppress.}

\item{show_plot}{Logical; draw the fitted density automatically. Default
\code{TRUE}.}

\item{show_summary}{Logical; print the summary immediately.}

\item{boot_iter}{Bootstrap replicates (500+ for reported intervals; required
for the forest plot). Set here \strong{or} in \code{\link[=zcurve_control]{zcurve_control()}}, not both.}

\item{control}{A list of tuning parameters from \code{\link[=zcurve_control]{zcurve_control()}}.}

\item{plot_control}{A list of \code{\link[=plot.zcurve]{plot.zcurve()}} options used for the automatic
plot and stored on the fit, so later \code{plot(fit)} calls reuse the same look;
an explicit \code{plot()} argument still overrides.}
}
\value{
An object of class \code{"zcurve"}: a list with the fitted estimates (ODR,
EDR, ERR, FDR) and mixture components, plus -- when \code{yi}/\code{sei} were supplied
-- an \code{effect_size} list and per-study \code{individual_effects}. Has
\code{\link[=plot.zcurve]{plot.zcurve()}}, \code{print}, and \code{summary} methods.
}
\description{
Fits a distribution-free mixture of non-central components to the significant
test statistics in a literature and returns power, publication-bias, and (when
effect sizes are supplied) effect-size estimates: the observed and expected
discovery rates (ODR, EDR), the expected replication rate (ERR), and the
false-discovery rate (FDR).
}
\details{
Supply exactly one consistent combination of test statistics via \code{zval},
\code{tval} + \code{df}, \code{pval}, or \code{yi} + \code{sei}. \code{yi}/\code{sei} may be added to any of the
others and are \strong{required} for the effect-size table and the forest plot.
}
\examples{
z <- abs(rnorm(2000, mean = 2.5))
fit <- zcurve(zval = z, show_plot = FALSE)
fit
}
\seealso{
\code{\link[=zcurve_control]{zcurve_control()}}, \code{\link[=zcurve_forest]{zcurve_forest()}}, \code{\link[=plot.zcurve]{plot.zcurve()}}
}
